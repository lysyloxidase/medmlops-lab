services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: ["make", "test"]
    environment:
      MEDMLOPS_ENV: ci
      OMP_NUM_THREADS: "1"
      MKL_NUM_THREADS: "1"

  postgres:
    ports: []

  mlflow:
    ports: []

  minio:
    ports: []
